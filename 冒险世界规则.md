# 冒险世界规则

> **版本**：2026-05-28 v1（世界规则定稿）  
> **适用范围**：阅读冒险地图、书架、每日目标、冒险家成长、探索发现及与宠物对战的联动  
> **关联文档**：[`产品规则定稿表.md`](./产品规则定稿表.md)（宠物/对战/经济）、[`docs/READING_REGION_MATRIX.md`](./docs/READING_REGION_MATRIX.md)（110 种书目分区）、[`PRODUCT_VISION.md`](./PRODUCT_VISION.md)（产品愿景）

---

## 0. 设计原则

| 原则 | 说明 |
|------|------|
| 阅读是钥匙 | 真实阅读 → 章节挑战 → 区域星星；不是「做题驱动」 |
| 世界是冒险 | 按**主题区域**闯关，**不按年级**分地图（多年级家庭可同区不同书） |
| 轻量每日 | 每日目标 3～5 分钟可完成，无考试压力 |
| 双成长线 | **冒险家**（主人）与**宠物**（伙伴）经验分开；货币统一 `users.coins` |
| 探索有回报 | 地图/关卡探索可掉落**对战道具**或收藏，与宠物页 PK 共用仓库 |

**状态标注**：文中「已实现」指当前代码可对齐；「规划」指规则已定、需按表实现或补全。

---

## 1. 核心闭环

```text
首页「继续冒险」
  → 冒险地图（选区域）
    → 区域书单（该区教育部推荐书目池）
      → 继续阅读（阅读器）或 直接挑战（每日限次）
        → 章节互动挑战（AI 冒险剧情）
          → 结算：区域星星 + 冒险家经验 + coins + 宠物经验
            → 推进区域「冒险之路」关卡
              → 解锁下一区域 / 触发探索发现
```

- 书架（`/library`）与区域书单**共用同一书目主池**（`MOE_CATALOG_2020`），书架侧重浏览与筛选，地图侧重闯关动机。
- 知识 PK（`/battle`）为**独立玩法**，消耗宠物三维状态；可通过探索发现获得对战道具增强 PK。

---

## 2. 名词表

| 名词 | 含义 |
|------|------|
| 冒险家 | 登录用户（主人），有昵称、头像、冒险家等级、coins |
| 冒险区域 | 地图上 6 个主题世界节点，各有书单与「冒险之路」 |
| 冒险之路 | 某区域内线性 **12 关**进度条，与书目数量解耦 |
| 区域星星 | 单次章节挑战结算获得的星数（读后 3 星 / 直进 1 星），累计驱动关卡与解锁 |
| 世界进度 | 全图已完成关卡 ÷ 总关卡（6×12=72），百分比展示（UI 称「世界进度」，勿与「探索发现」混淆） |
| 探索发现 | 地图隐藏点、区域里程碑、宝箱等，可奖励道具或称号 |
| 对战道具 | 商城「装备」类 SKU，在 PK 前选用，单次对战消耗 |

---

## 3. 书架规则

### 3.1 书目来源（已实现 · 元数据）

- **唯一权威清单**：教育部《中小学生阅读指导目录（2020 年版）》小学段 **110 种**。
- 数据结构：[`lib/library/moe-catalog-2020.ts`](./lib/library/moe-catalog-2020.ts)  
- 分区表：[`docs/READING_REGION_MATRIX.md`](./docs/READING_REGION_MATRIX.md)

每本书字段：`catalogSeq`、`gradeBand`（1-2 / 3-4 / 5-6）、`category`（人文/文学/自然/艺术）、`primaryRegionId`、`contentPhase`（p1/p2/p3）。

### 3.2 年级与区域的关系（双轨制）

| 轨道 | 规则 |
|------|------|
| **闯关（区域）** | 只认区域 + 星星 + 关卡；**不认年级** |
| **选书（书架/书单）** | `gradeBand` 仅用于标签、筛选、排序；**不**作为区域解锁条件 |

- 文案：区域名用「魔法森林」等，不用「一年级专区」；书封展示「推荐 1～2 年级」。
- 可选扩展（规划）：用户档案 `primaryGrade`（1～6）或 `readerProfiles[]`（多孩），用于默认展开推荐档位。

### 3.3 电子版与「即将上架」（已实现 · 展示）

| 阶段 | 范围 | 说明 |
|------|------|------|
| P0 | 110 条元数据 | 书架可搜可筛，无正文显示「即将上架」 |
| P1 | 森林/冰雪/沙漠等 | 每区逐步接入正文 + `challengeChapterId` |
| P2 | 深海遗迹 | 自然科学批次 |
| P3 | 天空王国、梦境之塔 | 外国文学 + 艺术 |

已接入正文示例：小王子、夏洛的网、绿野仙踪（演示）；目录书通过 `legacyReaderBookId` 映射阅读器。

### 3.4 书架入口

| 入口 | 路径 | 作用 |
|------|------|------|
| 书架首页 | `/library` | 全目录浏览、学段筛选、统计 |
| 全部书目 | `/library/books` | 110 种完整列表 |
| 阅读器 | `/library/read/[bookId]` | 章节阅读，完成后可跳冒险挑战 |
| 冒险区域书单 | `/adventure` 弹层 | 与书架同源，按 `primaryRegionId` 过滤 |

---

## 4. 六大冒险区域

配置单源：[`lib/library/adventure-regions.ts`](./lib/library/adventure-regions.ts)、[`lib/adventure/config.ts`](./lib/adventure/config.ts)

| 顺序 | ID | 名称 | 主题 | 书目约数 | 解锁条件 |
|------|-----|------|------|----------|----------|
| 1 | `magic-forest` | 魔法森林 | 童话、绘本、友情 | 20 | 默认解锁 |
| 2 | `ice-mountain` | 冰雪之巅 | 成长、冒险、品格 | 18 | 前区进度 ≥ 50% |
| 3 | `ancient-desert` | 远古沙漠 | 人文、历史、地理 | 22 | 前区进度 ≥ 50% |
| 4 | `ocean-ruins` | 深海遗迹 | 自然科学、科普 | 22 | 前区进度 ≥ 50% |
| 5 | `sky-kingdom` | 天空王国 | 外国名著、奇幻科幻 | 18 | 前区进度 ≥ 50% |
| 6 | `dream-tower` | 梦境之塔 | 高阶文学、艺术 | 10 | 前区进度 ≥ 50% |

- 解锁链：**仅看上一区域 `progress`（%）**，与年级、冒险家等级无关。
- 前端门槛提示由 `unlockWhenPrevProgressAtLeast`（默认 50）生成。

---

## 5. 区域「冒险之路」

### 5.1 关卡与星星（已实现 · 逻辑）

| 项 | 定稿值 |
|----|--------|
| 每区关卡数 `totalStages` | **12**（与区内书目数量无关） |
| 星星 → 关卡 | `completedStages = min(totalStages, 区域累计 stars)` |
| 区域进度 `progress` | `round(completedStages / totalStages × 100)`，上限 100% |

- **不必读完区内每一本书**；任意归属该区的书，完成挑战并获得星星，均可推进共用进度条。
- 兄妹同账号：共用一条区域进度（按登录账号）；若未来做「多读者档案」再议分轨。

### 5.2 星星获得（已实现）

来源：[`lib/adventure/config.ts`](./lib/adventure/config.ts) + [`lib/economy/reward-policy.ts`](./lib/economy/reward-policy.ts)

| 入口 | 模式 | 每次挑战星星 | 主人 coins | 宠物经验 |
|------|------|--------------|------------|----------|
| 读完章节再挑战 | `post_read` | **3** | 8 | 12 |
| 直接挑战（快捷） | `direct` | **1** | 4 | 6 |

- **直接挑战**：每日全账号上限 **3 次**（`DIRECT_CHALLENGE_DAILY_LIMIT`）。
- 结算 API：`POST /api/reading/complete`（须带 `regionId`、`bookId`、`challengeChapterId`）。
- **不扣宠物三维**：阅读章节挑战**仅**发放 coins、宠物经验、主人经验、区域星星；**不**调用 `battle/enter`，不消耗饱食/精神（与 [`产品规则定稿表.md`](./产品规则定稿表.md) §11 一致）。

### 5.3 关卡与书的映射（规划 · 体验规则）

| 规则 | 说明 |
|------|------|
| 默认 | 每完成 **1 次**有效章节挑战（获得星星）→ 区域累计星星 +N，由关卡公式自动折算进度 |
| 可选细化 | 长书可配置「每书 3 章 = 1 颗里程碑星」，在书目元数据增加 `stageWeight`（见 `READING_REGION_MATRIX` 扩展列） |
| BOSS 关（规划） | 每区第 12 关为**区域守护者挑战**，需本区进度 ≥ 92% 且完成 1 次读后挑战方可挑战 |

### 5.4 冒险之路 UI

- 组件：[`components/adventure/adventure-path.tsx`](./components/adventure/adventure-path.tsx)（关卡节点）。
- 详情页：[`app/adventure/progress/page.tsx`](./app/adventure/progress/page.tsx)（区域列表 + 成就 Tab）。

---

## 6. 冒险进度

### 6.1 三层进度（已实现 · 计算）

```text
单次挑战 → 写入 reading_records（含 region_id、stars_gain）
  → 汇总区域 stars
    → 推导 completedStages / progress
      → 推导 worldProgress（全图）
```

实现：[`lib/adventure/server-progress.ts`](./lib/adventure/server-progress.ts)

| 层级 | 展示名 | 计算 |
|------|--------|------|
| 区域 | 魔法森林 75% | 本区 `completedStages / 12` |
| 世界 | 世界进度 42% | 全区域 `completedStages` 之和 ÷ 72 |
| 星星 | 累计星星 | 各区域 `stars` 之和 |

### 6.2 与首页的关系（规划对齐）

- 首页「冒险进度」卡：当前区域名 + **世界进度**条 +「继续冒险」；**伙伴等级**在宠物卡展示，**冒险称号**弱展示于个人页。
- `users` 可选字段（规划持久化）：`adventure_level`、`world_progress`（或由服务端实时汇总，**以 reading_records 汇总为准**）。

---

## 7. 阅读 ↔ 挑战规则

### 7.1 推荐路径

```text
区域书单 → 继续阅读 → 读完一章 → 提示「去冒险」
  → 章节挑战（post_read，3 星）
```

### 7.2 快捷路径

```text
区域书单 → 直接挑战（direct，1 星，每日 ≤3 次）
```

- 适用：复习、重玩、时间紧；奖励低于读后挑战。
- 无正文的书不可阅读/不可挑战（即将上架）。

### 7.3 挑战内容

- 章节 JSON：`public/story/chapters/chapter_*.json`
- 每本书在 `REGION_SHELF_CONFIG.books[]` 绑定 `challengeChapterId`（目录书按 `chapter_{catalogSeq}` 占位，正文接入后替换）。

---

## 8. 每日目标

### 8.1 设计目标

- **轻量**：单项 3～5 分钟；全天完成 3 条每日任务约 10～15 分钟。
- **无压力**：不惩罚未完成；次日刷新。
- **引导闭环**：至少 1 条指向阅读或冒险，1 条指向宠物照料。

### 8.2 每日任务池（已实现 · 模板）

配置：[`lib/economy/daily-task-templates.ts`](./lib/economy/daily-task-templates.ts)  
发奖 coins 档位：小 **15** / 中 **25** / 大 **35**（与 [`产品规则定稿表.md`](./产品规则定稿表.md) §7.1 一致）

| 类型 | 示例任务 | 档位 | 完成条件 |
|------|----------|------|----------|
| `reading` | 阅读达人：阅读 1 个章节 | 中 | `max_progress: 1` |
| `question` | 知识问答：答对 5 题 | 中 | 5 次正确（规划接挑战/PK 统计） |
| `challenge` | 挑战高手：赢得 1 场 PK | 大 | PK 胜 1 场 |
| `pet` | 关爱伙伴：喂养宠物 1 次 | 小 | 互动 1 次 |

- 领取：`POST /api/tasks/claim` → `users.coins` + 任务配置的宠物经验。

### 8.3 首页「今日目标」展示（规划）

每日展示 **3 条**（从模板轮换或按行为缺口推荐）：

| 槽位 | 优先推荐逻辑 |
|------|----------------|
| 槽位 1 | 未完成：阅读 1 章 **或** 完成 1 次区域读后挑战 |
| 槽位 2 | 未完成：抚摸/喂食/玩耍/休息 任选 1 |
| 槽位 3 | 未完成：完成 1 次每日任务领取 **或** 冒险地图探索 1 个隐藏点 |

- 全部完成：额外宝箱（规划）：coins +5～10，或随机对战道具碎片。

### 8.4 每日刷新

- 任务进度按自然日（服务器时区）重置。
- 与宠物「每日衰减」独立：`POST /api/pets/daily-decay` 每日首次 -饱食 20 / -亲密 10。

---

## 9. 冒险家等级与称号

> **说明**：冒险家等级 = **主人**成长；宠物等级见产品规则 §6（`pet_level` / `pet_exp`，上限 50）。二者经验**分开累计**。

### 9.1 冒险家经验（Adventure XP）来源（规划）

| 行为 | 冒险家 XP | 备注 |
|------|-----------|------|
| 章节挑战 · 读后 | +20 | 与 `experienceGain` 对齐 |
| 章节挑战 · 直进 | +10 | |
| 每日任务领取 | +25～50 | 按任务 `xp_reward` |
| 探索发现 | +15～30 | 见 §11 |
| 区域通关（12/12 关） | +100 | 一次性 |
| PK 胜利 | +15 | 可选，鼓励跨玩法 |

- 存储建议：`users.adventure_xp` 或 `users.experience`（需与现有字段统一后实现）。

### 9.2 等级曲线（规划）

| 等级段 | 冒险家等级 | 累计 XP 阈值（建议） | 称号 |
|--------|------------|----------------------|------|
| 入门 | 1～5 | 0～400 | 见习冒险家 |
| 成长 | 6～15 | 401～2000 | 青铜冒险家 |
| 进阶 | 16～30 | 2001～6000 | 白银冒险家 |
| 精英 | 31～45 | 6001～12000 | 黄金守护者 |
| 传奇 | 46～60 | 12001+ | 钻石传说 / 至尊大师 |

- UI 参考：[`components/profile/adventurer-hero.tsx`](./components/profile/adventurer-hero.tsx) 的 `rankBadge` 阶梯。

### 9.3 等级权益（规划 · 仅展示与轻微加成）

| 权益 | 说明 |
|------|------|
| 称号展示 | 个人页、排行榜、好友卡片 |
| 地图装饰 | 高等级解锁头像框、地图足迹样式 |
| 经济 | **不**用等级硬锁区域；可选满级折扣：商城 95 折 |
| 对战 | **不**用冒险家等级算 ATK/HP（对战属性只看宠物等级） |

---

## 10. 成就系统

### 10.1 分类

| 类别 | 示例 | 触发数据 |
|------|------|----------|
| 阅读 | 读完 10 章、读完 5 本书 | `reading_records` / 阅读进度 |
| 冒险 | 累计 50 颗星、完成魔法森林 12 关 | `regionProgress` |
| 坚持 | 连续登录 7 天、连续阅读 3 天 | 登录/阅读日志 |
| 探索 | 发现 5 个地图秘密、开启 3 个宝箱 | `exploration_log`（规划表） |
| 社交 | 好友榜前 10、关注满 10 人 | 社交模块 |
| 宠物 | 宠物达到 25 级、对战 10 胜 | `pets` / PK 记录 |

### 10.2 成就奖励（规划）

| 奖励类型 | 说明 |
|----------|------|
| 徽章 | 成就墙展示，无战力 |
| coins | 一次性 10～50 |
| 对战道具 | 指定成就送 1 件装备 SKU（进 `pet_inventory`） |
| 探索点数 | 仅用于收藏页等级，不影响战斗数值 |

### 10.3 与 UI 入口

- 冒险进度页 Tab「成就」：[`app/adventure/progress/page.tsx`](./app/adventure/progress/page.tsx)（当前为示例数据，需接成就表）。
- 个人页：[`components/profile/achievement-wall.tsx`](./components/profile/achievement-wall.tsx)

---

## 11. 探索发现

### 11.1 类型

| 类型 | 触发方式 | 出现位置 |
|------|----------|----------|
| 地图隐藏点 | 点击地图光点 / 探索度达标 | [`world-map.tsx`](./components/adventure/world-map.tsx) 隐藏点 |
| 区域里程碑 | 本区 progress 达 25/50/75/100% | 区域冒险之路节点 |
| 宝箱 | 本区累计 3 次读后挑战 / 通关 | 冒险页「探索奖励」区 |
| 秘密事件 | 连续 3 天阅读后首次进图 | 剧情气泡（规划） |

### 11.2 与对战道具联动（核心）

对战道具配置：[`lib/pets/battle-items.ts`](./lib/pets/battle-items.ts)（装备类 SKU，与商城 `equipment` 类目一致）

| 探索发现 | 奖励 | 对战效果 |
|----------|------|----------|
| 森林遗迹箱 | `equip-bandana` ×1 | 应急药剂：低血量回血 |
| 冰晶洞窟 | `equip-tag` ×1 | 守护护盾：抵挡 1 次伤害 |
| 沙漠古城 | `equip-badge` ×1 | 破甲符文：首次攻击破防 30% |
| 深海气泡 | `equip-charm` ×1 | 反击棱镜：反弹 25% 伤害 |
| 天空浮岛 | `equip-collar` ×1 | 强攻护符：首次答对 +攻击 |
| 梦境阶梯 | `equip-bell` ×1 | 连击推进器：连击 +1 |

**规则**：

1. 探索奖励道具进入 **`pet_inventory`**（与商城购买相同）。
2. PK 前在 [`/battle`](./app/battle/page.tsx) 选用最多 **3 件**（与现 UI 一致），对战结束消耗已触发次数的道具。
3. **冒险章节挑战**（阅读冒险）规划可选：允许携带 **1 件**探索道具，仅本场生效（P2 功能）。
4. 同一发现全账号一次性；重复探索只给 coins 或冒险家 XP。

### 11.3 探索发现 UI

- 区块：[`components/adventure/reward-discovery.tsx`](./components/adventure/reward-discovery.tsx)（宝箱、秘密、收集册入口）。
- 收藏册：[`app/adventure/collection/page.tsx`](./app/adventure/collection/page.tsx)

### 11.4 数据（规划）

建议表 `exploration_discoveries`：

| 字段 | 说明 |
|------|------|
| `user_id` | 用户 |
| `discovery_id` | 如 `map-forest-chest-1` |
| `region_id` | 可选 |
| `claimed_at` | 领取时间 |
| `reward_type` | `coins` / `battle_item` / `badge` |
| `reward_payload` | JSON，如 `{ "itemId": "equip-tag" }` |

---

## 12. 奖励与经济衔接

所有 **coins** 写入 `users.coins`（界面可称冒险积分/金币）。总表：产品规则 §7 + [`lib/economy/reward-policy.ts`](./lib/economy/reward-policy.ts)

| 玩法 | coins（已实现） |
|------|-----------------|
| 读后挑战 | 8 |
| 直进挑战 | 4 |
| 每日任务 | 15 / 25 / 35 |
| PK 胜/平/负 | 12 / 6 / 3 |

**日活参考**：积极玩家每日 coins 收入约 100+，与宠物照料、对战道具价格（22～30）匹配，约 2 天可买 1 件对战道具。

---

## 13. 与宠物 / PK 的边界

| 维度 | 阅读冒险 | 知识 PK |
|------|----------|---------|
| 入口 | `/adventure` | `/battle` |
| 驱动 | 阅读章节 + 剧情挑战 | 答题对战 |
| 消耗三维 | **不扣**饱食/精神/亲密 | 入场 `battle/enter` 或结算各 **-10** 饱食、**-10** 精神（见产品规则 §2、§10） |
| 消耗道具 | 无（V1）；规划 P2 可携带 1 件 | 战前最多 3 件，战后消耗 |
| 进度 | 区域星星、冒险之路、世界进度 | 胜场 `battle_wins`、排行榜 |
| 道具来源 | 探索发现 → 仓库（**仅 PK 用**） | 战前选用、战后消耗 |
| 经验 | 宠物经验 + 主人经验 + coins | 宠物经验 + coins |
| 战力 | 不影响 ATK/HP | 宠物等级 + 装备决定 ATK/HP |

- 宠物三维、死亡、重新领养：**完全遵循**产品规则定稿表，冒险世界不例外。
- 排行榜胜场：仅 PK 胜利累加；**区域星星不计入**对战排行榜。

### 13.1 用语统一（产品文案）

| 统一用词 | 用途 |
|----------|------|
| **冒险** | 地图、Tab、挑战、连续天数、冒险等级、冒险家 |
| **世界进度** | 全图关卡完成 %（勿写作「探索进度」） |
| **探索发现** | 仅指地图隐藏点、宝箱、里程碑奖励 |
| **伙伴等级** | 宠物 `pet_level`（首页主展示） |
| **冒险称号** | 主人段位（青铜冒险家等，个人页展示） |

少用「探险」；「探索」不与「世界进度」混用。

---

## 14. 实现对照清单

| 模块 | 关键路径 | 状态 |
|------|----------|------|
| 书目目录 | `lib/library/moe-catalog-2020.ts` | 已实现 |
| 区域配置 | `lib/adventure/config.ts` | 已实现 |
| 进度汇总 | `lib/adventure/server-progress.ts` | 已实现 |
| 阅读结算 | `app/api/reading/complete/route.ts` | 已实现（不扣三维） |
| PK 入场扣状态 | `app/api/battle/enter/route.ts` | 已实现（仅 PK） |
| 每日任务 | `lib/economy/daily-task-templates.ts` | 已实现 |
| 对战道具 | `lib/pets/battle-items.ts` | 已实现 |
| 冒险家等级持久化 | `users.adventure_xp` | 规划 |
| 成就表 | DB + API | 规划 |
| 探索发现领取 | DB + API | 规划 |
| 区域 BOSS 关 | 剧情 + 结算 | 规划 |

---

## 15. 分期落地建议

### 15.1 V1 范围（当前主打，控制复杂度）

| 包含 | 不包含（推迟） |
|------|----------------|
| 阅读 → 章节挑战 → 星星 / 世界进度 | 章节挑战携带对战道具 |
| 宠物照料 + 每日 3 类任务（阅读 / PK / 喂养） | `question` 类每日任务（未接统计前不 seed） |
| PK 可选；coins 统一经济 | 6 区全量探索宝箱、多套成就 UI 并行 |
| 首页：伙伴等级 + 世界进度 + 继续冒险 | 区域 BOSS、多孩 `readerProfiles` |
| 探索发现 UI 可保留占位，**不**作为每日必做 | 冒险称号全站大字、第二套货币 |

详见 [`docs/V1_LAUNCH_CHECKLIST.md`](./docs/V1_LAUNCH_CHECKLIST.md) 第 8 节。

### 15.2 阶段 A→E

| 阶段 | 内容 |
|------|------|
| **A** | 维持现有：110 目录、6 区、星星进度、阅读/PK 分轨 |
| **B** | 冒险家 XP + 等级称号写库；首页今日目标与任务进度打通 |
| **C** | 成就系统接 `reading_records` / 区域进度（单入口） |
| **D** | 探索发现表 + 领取 API + 收藏册同步 |
| **E** | 区域 BOSS 关、章节挑战携带 1 道具、多孩 `readerProfiles` |

---

## 16. 修订记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-05-28 | v1 | 首版：整合区域规划、每日目标、冒险家等级、成就、探索发现与对战道具联动 |
| 2026-05-28 | v1.1 | 阅读不扣三维；§13 区分阅读/PK；用语统一；V1 范围表 |

---

*本文档为冒险世界玩法总纲。数值以 [`产品规则定稿表.md`](./产品规则定稿表.md) 与 [`lib/economy/reward-policy.ts`](./lib/economy/reward-policy.ts) 为准；书目分区以 [`docs/READING_REGION_MATRIX.md`](./docs/READING_REGION_MATRIX.md) 为准。冲突时：经济/宠物/对战 → 产品规则定稿表；区域与书目 → 本表 + 区域矩阵。*
