# 冒险知识 · 产品规则定稿表（v2）

> **版本**：2026-05-27 v2（规则已定稿，代码已对齐；部署见 `docs/DEPLOYMENT.md`）  
> **适用范围**：`v0-app` 客户端 + Supabase `users` / `pets` 及相关 API  
> **货币**：全玩法奖励统一写入 **`users.coins`**（界面可称「冒险积分」/「金币」）；不再新增平行余额字段。  
> **饱食度**：界面与逻辑用 `satiety`；数据库列名仍为 `hunger` / `pet_hunger`（兼容）。  
> **冒险世界**：地图、书架、星星进度、探索发现见 [`冒险世界规则.md`](./冒险世界规则.md)；**经济/宠物/对战数值以本文档为准**。

---

## 0. 与冒险世界规则的关系

### 0.1 文档分工

| 文档 | 权威范围 |
|------|----------|
| **本文档（产品规则定稿表）** | 宠物三维、死亡、对战、商城、`users.coins`、`pet_exp`、每日衰减、PK 扣状态 |
| [`冒险世界规则.md`](./冒险世界规则.md) | 六大区域、110 种书目、星星→关卡、世界进度、探索发现叙事、每日目标结构 |
| [`lib/economy/reward-policy.ts`](./lib/economy/reward-policy.ts) | 各玩法 coins / petExp **数值单源** |

**冲突时**：coins、宠物状态、对战属性 → **本文档 + reward-policy**；区域解锁、书目分区 → **冒险世界规则 + READING_REGION_MATRIX**。

### 0.2 双成长线（勿混为一谈）

| 线 | 字段 | 上限 | 影响战斗 | 首页展示 |
|----|------|------|----------|----------|
| **伙伴** | `pet_level` / `pet_exp` | 50 级（§6） | **是**（ATK/HP） | **主展示**（宠物卡） |
| **冒险家（主人）** | `users.level` + 经验字段 | 规划称号档 | **否** | **弱展示**（个人页称号） |

- 阅读 `POST /api/reading/complete`：发 **pet_exp** + **coins** + 主人经验；**不扣**饱食/精神。
- PK `POST /api/battle/enter` + `settle`：发 **pet_exp** + **coins**；**扣**饱食/精神（§2、§10）。

### 0.3 冒险称号（主人段位 · UI）

与 [`components/profile/adventurer-hero.tsx`](./components/profile/adventurer-hero.tsx) 一致：

| 档位 | 称号 |
|------|------|
| bronze | 青铜冒险家 |
| silver | 白银冒险家 |
| gold | 黄金守护者 |
| diamond | 钻石传说 |
| master | 至尊大师 |

低等级可称「见习冒险家」（见冒险世界规则 §9）。称号**不**参与 ATK/HP 计算。

### 0.4 已废弃：用户体力（energy）

- 早期原型有 `users.energy` / 界面闪电条；**v2 定稿无用户体力玩法**。
- 宠物仅认三维：**饱食度、精神值、亲密值**（§1）；PK 消耗饱食/精神，阅读挑战不扣。
- UI 勿再展示「体力/energy」；书架/首页经验条表示 **主人冒险进度**（`users.experience` 本级进度）。

### 0.6 连续冒险天数（`daily_streak`）

| 项 | 定稿（V1 目标） |
|----|----------------|
| 含义 | 用户**连续有活跃行为**的自然日计数，首页展示「连续 N 天」 |
| 活跃判定（建议） | 当日满足任一：登录并进入首页、完成 1 次阅读挑战、领取 1 个每日任务 |
| 刷新 | 每日 0 点（服务器时区）结算：昨日有活跃 → +1 或保持；昨日无活跃 → **归零** |
| 存储 | `users.daily_streak`（兼容 `streak` / `reading_streak`） |
| UI | 7 个小圆点表示本周节奏（亮 = 已达成的连续天内，最多 7 个） |
| 奖励 | V1 **仅展示**，不断签惩罚；成就「连续冒险 7 天」等后续接表 |

**实现状态（2026-05）**：`lib/user/daily-streak-server.ts` + `POST /api/users/daily-activity`；首页进入、阅读结算、任务领取时写库（幂等，同日不重复 +1）。

### 0.7 星星 vs 主人等级

- **收集星星**：章节挑战产出，驱动区域关卡与世界进度；与宠物战力无关。
- **冒险等级**：主人经验累计，与星星**同次挑战一并获得**，但**无**「N 颗星 = Lv.N」换算。
- **世界进度**：全图 `completedStages` ÷ 72，UI 统一称「世界进度」。

### 0.8 神秘宝藏（书架收集）

| 项 | 定稿 |
|----|------|
| 含义 | 长期收集奖励（宠物外观、称号、地图、道具等），**不影响对战数值** |
| 配置 | `treasure_definitions`（条件类型 + 目标值 + 文案） |
| 解锁记录 | `user_treasures`（`user_id` + `treasure_id` + `unlocked_at`） |
| 进度计算 | 实时从 `reading_records`、区域进度、`users.daily_streak` 等推导，满 100% 自动写入解锁 |
| 同步时机 | 登录活跃、阅读结算、领任务、打开 `GET /api/treasures` |
| 连续天数类条件 | 统一用 `daily_streak`（连续活跃），文案「连续活跃 N 天」 |

**条件类型**：`stars_total` / `daily_streak` / `book_complete` / `books_read_count` / `post_read_books_count` / `regions_unlocked` / `region_progress` / `world_progress`。

**实现**：`lib/treasures/*`、`GET /api/treasures`、书架 `UnlockableRewards` + `/library/treasures`。

---

## 变更摘要（相对 v1）

| 项 | v2 定稿 |
|----|---------|
| 对战主人奖励 | **入账 `users.coins`**，胜场 **12** |
| 宠物等级 | **方案 A：线性递增升级经验**，封顶 **50 级**，生命阶段 **1–10 / 11–25 / 26–50** |
| 心情 | 仅 **四态** + 2D 映射；死亡 chip 显示 **「离世」** |
| 每日衰减 | 饱食 **-20**（`DAILY_SATIETY_DECAY`）；亲密 **-10**（`DAILY_BOND_DECAY`） |
| 死亡 | **无**复活/救治道具，仅重新领养 |
| 互动 | 玩家 **自选仓库 SKU**，API 传 `itemId` |
| 战斗门槛 | 饱食 **≥20**、精神 **≥10**、亲密 **≥10**、未死亡 |
| 战斗属性 | 等级驱动 **ATK / HP**，阶段系数见 §9 |

---

## 1. 宠物三维状态

| 维度 | 逻辑字段 | 数据库字段（兼容） | 范围 | 初始值 |
|------|----------|-------------------|------|--------|
| 饱食度 | `satiety` | `hunger` / `pet_hunger` | 0–100 | **80** |
| 精神值 | `spirit` | `spirit` / `pet_spirit` / `energy` | 0–100 | **80** |
| 亲密值 | `bond` | `bond` / `pet_bond` / `happiness` / `intimacy` | 0–100 | **80** |

- 写入前 `clampPetState` 限制在 0–100。
- bootstrap、首次 `setup`、**重新领养** 后三维均为 **80**。

---

## 2. 状态衰减

| 触发 | 饱食度 | 亲密值 | 精神值 |
|------|--------|--------|--------|
| **每日首次**（首页/宠物页 → `POST /api/pets/daily-decay`） | **-20** | **-10** | — |
| **对战结算一次** | **-10** | — | **-10** |

- 常量名：**`DAILY_SATIETY_DECAY = 20`**（保留，不改为 HUNGER 命名）；**`DAILY_BOND_DECAY = 10`**。
- 通过 `last_daily_decay_date` 保证每日只扣一次。
- **仅饱食度 ≤ 0** 触发死亡（`isDead`）；亲密/精神过低不致死。

---

## 3. 战斗入场门槛

须 **同时满足**（前端 `canEnterBattle` + 后端 `battle/settle` 双端校验）：

| # | 条件 | 失败提示要点 |
|---|------|----------------|
| 1 | 未死亡 | 已离世，无法战斗 |
| 2 | 饱食度 **≥ 20** | 饱食度不足 |
| 3 | 精神值 **≥ 10** | 精神值不足 |
| 4 | 亲密值 **≥ 10** | 亲密值不足 |

常量建议：`MIN_SATIETY_FOR_BATTLE = 20`、`MIN_SPIRIT_FOR_BATTLE = 10`、`MIN_BOND_FOR_BATTLE = 10`。

---

## 4. 死亡与重新领养

### 4.1 死亡

- **饱食度 ≤ 0** → `is_dead = true`，记录 `dead_at`。
- **不提供**任何复活、救治、还魂类商城道具或互动效果。

### 4.2 死亡弹窗与心情

- `isDead` 时 **立即弹出** 死亡弹窗（不可遮罩关闭）；坟头图见 `public/image/pets dead/`（狗/猫/鸟，其他物种回退狗）。
- 心情 Chip：固定文案 **「离世」**（不进入饥饿/无聊/犯困/开心四态）。
- 按钮「重新领养宠物」→ `/auth/pet-setup?readopt=1`。

### 4.3 重新领养（`POST /api/pets/readopt`）

- 重选物种、子品种、名字；三维 → **80**；`pet_level = 1`、`pet_exp = 0`、`life_stage = 幼崽`；清空背包；清除死亡标记。

### 4.4 死亡期间禁止

- 喂食 / 训练 / 休息 / 玩耍、商城购买。

---

## 5. 心情（唯一展示规则）

**唯一来源**：`getHomePetMood`（阈值 **50**，优先级从高到低）：

| 优先级 | 条件 | 展示 | `HomePetMood` |
|--------|------|------|----------------|
| — | `isDead` | **离世** | （不返回四态，UI 专用） |
| 1 | 饱食度 < 50 | 饥饿 | `hungry` |
| 2 | 亲密值 < 50 | 无聊 | `bored` |
| 3 | 精神值 < 50 | 犯困 | `sleepy` |
| 4 | 否则 | 开心 | `happy` |

### 5.1 2D 动画映射（`Pet2DScene.mood`）

| HomePetMood | 2D `mood` | 说明 |
|-------------|-----------|------|
| happy | `happy` | 默认 idle；互动成功可短时 `excited` |
| hungry | `hungry` | 饥饿态素材 |
| bored | `listless` | 无 bored 专用素材时用无精打采 |
| sleepy | `sleepy` | 犯困/打盹 |
| 离世 | — | 不播常规 mood 循环，由弹窗承接 |

**废弃**：`getPetMoodByVitals` 的 sad/cute 等独立体系；宠物页 mood 由 `getHomePetMood` 推导。

---

## 6. 宠物等级与生命阶段（全玩法统一 · 方案 A）

| 项目 | 定稿 |
|------|------|
| 升级曲线 | **线性递增**：等级越高，升下一级所需经验越多 |
| 等级上限 | **50**（`pet_exp` 达到满级阈值后不再升级，经验可继续累计或封顶） |
| 实现单源 | `lib/pets/level-progress.ts`（建议）：`expToNextLevel` / `levelFromTotalExp` / `progressInLevel` |

### 6.1 单级所需经验（方案 A）

当前等级为 `L`（`1 ≤ L ≤ 49`）时，升到 `L + 1` 所需经验：

```text
expToNext(L) = PET_EXP_BASE + (L - 1) × PET_EXP_STEP
             = 80 + (L - 1) × 8
```

| 常量 | 值 | 含义 |
|------|-----|------|
| `PET_EXP_BASE` | **80** | 1→2 级所需经验 |
| `PET_EXP_STEP` | **8** | 每升一级，下一档需求 +8 |
| `PET_LEVEL_CAP` | **50** | 最高等级 |

**示例单级需求**：1→2 需 **80**；10→11 需 **152**；25→26 需 **272**；49→50 需 **464**。

### 6.2 累计经验与等级反查

到达等级 `L`（`L ≥ 2`）所需的 **累计** `pet_exp` 下限：

```text
cumulativeExp(L) = Σ expToNext(n)  （n 从 1 到 L-1）
                 = (L - 1) × 80 + 8 × (L - 2) × (L - 1) / 2
```

- `cumulativeExp(1) = 0`（出生为 1 级，0 经验）。
- 到达 **50 级** 至少需：`cumulativeExp(50) = **13,328**` 总经验。

由总经验 `E` 求等级（伪代码）：

```text
level = 1
while level < 50 and E >= cumulativeExp(level + 1):
  level += 1
```

### 6.3 当前级进度条

```text
progress% = (E - cumulativeExp(level)) / expToNext(level) × 100
```

- 首页、宠物页、对战结果等 **禁止** 使用 `E % 100`；统一调用 `progressInLevel(E)`。

### 6.4 阶段累计经验（节奏参考）

| 里程碑 | 累计经验 | 说明（约 90 经验/日） |
|--------|----------|------------------------|
| 达到 11 级（成年） | **1,160** | 约 **13 天** |
| 达到 26 级（壮年） | **4,400** | 约 **49 天** |
| 达到 50 级（满级） | **13,328** | 约 **148 天**（≈5 个月） |

> 以上为活跃玩家粗算（2 胜 + 1 读后挑战 + 任务等）；实际随任务宠物经验配置浮动。

### 6.5 生命阶段（与等级绑定）

| 阶段 | 等级 | `life_stage` | 立绘/头像资源 |
|------|------|--------------|----------------|
| 幼崽 | **1–10** | `幼崽` | stage-1 |
| 成年 | **11–25** | `成年` | stage-2 |
| 壮年 | **26–50** | `壮年` | stage-3 |

- `resolveBattleLifeStage(level)`：**≤10 幼崽，11–25 成年，≥26 壮年**（10 级仍幼崽，11 级起成年）。
- 阅读 `reading/complete`、任务 `tasks/claim`、对战 `battle/settle`、首页进度条 **共用** §6.1–6.3 等级工具（**不再**使用固定 100/级）。

---

## 7. 积分经济：玩法奖励总表

**配置位置（实现目标）**：`lib/economy/reward-policy.ts`（或等价单文件）；发奖只读此表。

### 7.1 主人 `users.coins`

| 玩法 | 触发 | coins | 宠物经验 | 备注 |
|------|------|-------|----------|------|
| 对战·胜 | `battle/settle` | **12** | 20 | 首胜额外宠物经验 **+10** |
| 对战·平 | 结算 | **6** | 12 | |
| 对战·负 | 结算 | **3** | 8 | |
| 阅读·读后挑战 | `reading/complete` `post_read` | **8** | 12 | |
| 阅读·直进挑战 | `direct` | **4** | 6 | 每日直进上限 **3** 次 |
| 每日任务·小 | `tasks/claim` | **15** | 按任务配置 | |
| 每日任务·中 | claim | **25** | 按任务配置 | |
| 每日任务·大 | claim | **35** | 按任务配置 | |

- **对战胜场 coins = 12**，结算时与宠物状态更新一并写入 `users.coins`。
- 任务 pet 经验仍可由 `daily_tasks` 行配置；coins 建议与上表 seed 一致。

### 7.2 日经济参考（活跃玩家模型）

| 类型 | 估算 coins/日 |
|------|----------------|
| 收入（3 任务 + 1 读后挑战 + 2 场胜） | 15+25+35+8+24 ≈ **107** |
| 支出（补饱食约 2 次 + 精神/亲密各 1 次消耗品） | ≈ **56** |
| 结余 | ≈ **50**（约 2 天一件对战道具档） |

- 商城消耗品单价 **10–19**、对战道具 **22–30**；v2 **不大调单价**，靠任务档位调节松紧。

---

## 8. 商城与互动

- UI 全局：**饱食度**（非「饥饿值」）；商品 `effect.satiety`（兼容 `effect.hunger`）。
- **无**复活/救治类商品。
- **互动消耗**：玩家从仓库 **自选 SKU**（同类目）；`POST /api/pets/interact`  body：`{ action, itemId }`；服务端校验类目、库存 ≥1 后扣减并应用该商品 `effect`。
- 仅 1 个 SKU 时可跳过选择器；可记住上次 `itemId`（localStorage）。

---

## 9. 对战属性：ATK、HP 与等级成长

**实现文件**：`lib/battle/rules.ts` — `calcBattleStats`、`calcBattleDamage`。

### 9.1 基础值（按等级线性成长）

设 `L = clamp(level, 1, 50)`：

```text
baseAttack(L)  = 18 + L × 3
baseHealth(L)  = 120 + L × 18
```

### 9.2 生命阶段系数 `stageFactor(L)`

与 §6.1 等级段一致：

| 阶段 | 等级 | stageFactor |
|------|------|-------------|
| 幼崽 | 1–10 | **0.95** |
| 成年 | 11–25 | **1.00** |
| 壮年 | 26–50 | **1.08** |

### 9.3 装备加成

```text
ATK  = floor( baseAttack(L)  × stageFactor × (1 + ΣatkPct + ΣallPct) )
HP   = floor( baseHealth(L)  × stageFactor × (1 + ΣhpPct  + ΣallPct) )
```

- 装备百分比来自已穿戴装备配置（对战外入口穿戴，对战内可读快照）。

### 9.4 关键等级数值表（无装备）

| 等级 | 阶段 | baseATK | baseHP | 实际 ATK | 实际 HP |
|------|------|---------|--------|----------|---------|
| 1 | 幼崽 | 21 | 138 | **19** | **131** |
| 10 | 幼崽 | 48 | 300 | **45** | **285** |
| 11 | 成年 | 51 | 318 | **51** | **318** |
| 25 | 成年 | 93 | 570 | **93** | **570** |
| 26 | 壮年 | 96 | 588 | **103** | **635** |
| 50 | 壮年 | 168 | 1020 | **181** | **1101** |

> 11 级、26 级为阶段切换点，ATK/HP 会有系数台阶，属设计预期。

### 9.5 单次伤害计算

```text
damage = max(1, floor( ATK × randomFactor × comboFactor × protectFactor × (1 + extraDamagePct) ))
```

| 因子 | 规则 |
|------|------|
| `randomFactor` | 均匀 **[0.9, 1.1]** |
| `comboFactor` | 连击 ≤1 → 1；否则 `1 + min(3, streak-1) × 0.1`（最高 +30%） |
| `protectFactor` | 攻方等级 − 守方等级 **> 8** 时 **0.92**，否则 1 |
| `extraDamagePct` | 对战道具等临时加成 |

### 9.6 对战节奏常量

| 常量 | 值 |
|------|-----|
| 回合上限 | **20** |
| 答题时限 | **12** 秒 |

### 9.7 与养成状态的联动

- 入场门槛（§3）限制低饱食/低精神/低亲密宠物参战。
- 每场结束：饱食 **-10**、精神 **-10**（§2），并发放 §7.1 经验与 coins。
- 等级提升仅改变 §9.1–9.4 的 ATK/HP，**不**自动回满三维状态。

---

## 10. 对战结算状态扣减（重申）

每场 `battle/settle` 成功后：

- `pet_exp` 增加（按胜负表）→ 按 **§6 方案 A** 重算 `pet_level` / `life_stage`
- 饱食度 **-10**、精神值 **-10**
- `users.coins` 增加（按 §7.1）
- 若饱食度 ≤ 0 → 死亡态

---

## 11. 关键 API

| 接口 | 方法 | 作用 |
|------|------|------|
| `/api/pets/daily-decay` | POST | 每日饱食 -20、亲密 -10 |
| `/api/battle/settle` | POST | 经验、coins、饱食/精神扣减 |
| `/api/pets/readopt` | POST | 重新领养 |
| `/api/pets/setup` | POST | 首次创建宠物 |
| `/api/pets/interact` | POST | `{ action, itemId }` 互动 |
| `/api/pets/shop/purchase` | POST | 扣 coins、加库存 |
| `/api/tasks/claim` | POST | 任务 coins + 宠物经验 |
| `/api/reading/complete` | POST | 阅读挑战 coins + 宠物经验 + 主人经验；**不扣**三维 |
| `/api/battle/enter` | POST | PK 入场扣饱食/精神各 10 |
| `/api/pets` | GET | `summary.state` 等 |
| `/api/users/daily-activity` | POST | 记录当日活跃，更新 `daily_streak` / `last_active_date` |
| `/api/treasures` | GET | 同步并返回神秘宝藏列表（含进度与解锁） |

---

## 12. 常量速查（实现对照）

### `lib/pets/state.ts`

```text
DEFAULT_PET_VITAL = 80
DAILY_SATIETY_DECAY = 20
DAILY_BOND_DECAY = 10
BATTLE_SATIETY_COST = 10
BATTLE_SPIRIT_COST = 10
MIN_SATIETY_FOR_BATTLE = 20
MIN_SPIRIT_FOR_BATTLE = 10
MIN_BOND_FOR_BATTLE = 10
HOME_MOOD_THRESHOLD = 50
PET_EXP_BASE = 80
PET_EXP_STEP = 8
PET_LEVEL_CAP = 50
// 满级累计经验 cumulativeExp(50) = 13328
```

### `lib/pets/level-progress.ts`（建议新建）

```text
expToNextLevel(L)      // L: 当前等级 1..49
cumulativeExpForLevel(L) // 到达 L 级所需总经验，L>=1
levelFromTotalExp(E)   // 1..50
progressInLevel(E)     // 0..100，当前级内进度
```

### `lib/battle/rules.ts`

```text
BASE_ATTACK_START = 18
BASE_ATTACK_GROWTH = 3
BASE_HEALTH_START = 120
BASE_HEALTH_GROWTH = 18
// 阶段系数：幼崽 0.95 | 成年 1.0 | 壮年 1.08
// 阶段边界：≤10 | 11–25 | ≥26
```

---

## 13. 数据库与部署

- 迁移：`20260530120000_pet_vitals_and_daily_decay.sql`（饱食默认 80、`last_daily_decay_date` 等）。
- 已有用户：按 `pet_exp` 用 **§6 方案 A** 重算 `pet_level`（封顶 50）与 `life_stage`。
- **代码已实现（2026-05-27）**：`lib/pets/level-progress.ts`、`lib/economy/reward-policy.ts`、对战 coins 入账、阶段 11/26、亲密每日衰减、互动 `itemId` + 仓库选择器、战斗门槛、心情/离世 Chip、方案 A 进度条。
- **部署步骤（详见 `docs/DEPLOYMENT.md`）**：
  1. Supabase SQL Editor 执行 `supabase/scripts/apply-v2-migrations-bundle.sql`（或按序执行 `migrations/` 下 4 个 v2 相关文件）
  2. 可选：`pnpm pets:recalc-levels` 或 `POST /api/dev/recalc-pet-levels`
  3. `pnpm build` 后部署前端
- **新用户任务 seed**：`lib/economy/daily-task-templates.ts`（coins 15 / 25 / 35）

---

## 14. 已关闭问题（不再讨论）

- ~~对战是否入账~~ → **是，胜 12**
- ~~固定 100 经验/级~~ → **方案 A 线性递增（80 + (L-1)×8）**
- ~~心情双体系~~ → **仅四态 + 2D 映射**
- ~~复活道具~~ → **无**
- ~~自动消耗第一件~~ → **玩家自选 SKU**

---

*本文档为 v2 产品定稿。代码实现与 Supabase 部署须在本文档确认后进行，并保持与 `lib/economy/reward-policy.ts`、`lib/pets/state.ts`、`lib/battle/rules.ts` 单源一致。*
